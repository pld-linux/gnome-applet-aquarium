--- shermans_aquarium-2.2.0/src/Makefile.in.orig	Wed Aug 14 09:29:28 2002
+++ shermans_aquarium-2.2.0/src/Makefile.in	Wed Sep  3 14:02:11 2003
@@ -50,12 +50,11 @@
 
 # Setting up the CFLAGS and LDFLAGS variables 
 
-CFLAGS += -Wall -O3 -DVERSION="\"$(SHERMANS_AQUARIUM_VERSION)\"" -DDATADIRPREFIX="\"$(DATADIR)\""
+CFLAGS = @CFLAGS@ -Wall -DVERSION="\"$(SHERMANS_AQUARIUM_VERSION)\"" -DDATADIRPREFIX="\"$(DATADIR)\"" \
+	-D$(HOST) @USE_GNOME@ @WITH_THERMO@ @WITH_CLOCK@ @WITH_LEDS@ @WITH_MATRIX@ @GTK2@ @FULLSCREEN@ \
+	$(GTK_CFLAGS) $(GDK_PIXBUF_CFLAGS) $(@USE_GNOME_CFLAGS@) $(@USE_ORBIT_CFLAGS@) $(@USE_SDL_CFLAGS@)
 
-CFLAGS += -D$(HOST) @USE_GNOME@ @WITH_THERMO@ @WITH_CLOCK@ @WITH_LEDS@ @WITH_MATRIX@ @GTK2@ @FULLSCREEN@
-CFLAGS += $(GTK_CFLAGS) $(GDK_PIXBUF_CFLAGS) $(@USE_GNOME_CFLAGS@) $(@USE_ORBIT_CFLAGS@) $(@USE_SDL_CFLAGS@)
-
-LDFLAGS += $(GTK_LIBS) $(GDK_PIXBUF_LIBS) $(@USE_GNOME_LDFLAGS@) $(@USE_SDL_LIBS@) $(SYS_SPEC_LIBS)
+LDFLAGS = @LDFLAGS@ $(GTK_LIBS) $(GDK_PIXBUF_LIBS) $(@USE_GNOME_LDFLAGS@) $(@USE_SDL_LIBS@) $(SYS_SPEC_LIBS)
 
 OBJS = aquarium.o @SYSLOAD_OBJ@ @KLEDS_OBJ@ draw.o wmapplet.o detectbrokenwm.o @GNOME_OBJ@ @PREF_OBJ@ @MATRIX_OBJ@ @GETOPT_OBJ@ @FULLSCREEN_OBJ@
 
@@ -64,7 +63,6 @@
 
 sherman: stamp.h $(OBJS)
 	$(CC) $(LDFLAGS) -o sherman $(OBJS) 
-	strip sherman
 
 install: sherman @INSTALL_CHOISE@
 
